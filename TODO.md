# TODO

本文件只记录尚未完成、计划在后续版本处理的事项。审核发现的问题使用“审核发现”标记，并附上可复查的文件或验证结果；事项完成后从本文件移除，同时在 `CHANGELOG.md` 的当前版本中记录实际修改。

## 下一版本（未发布）

### 审核发现

- [ ] **中优先级**：评估 Material for MkDocs 关于 MkDocs 2.0 的兼容性提醒；在依赖升级说明中保留当前 `<2.0` 约束，确认部署流程不会意外升级到不兼容版本。验收：依赖说明与部署安装命令均维持兼容版本。证据：`python -m mkdocs build` 的上游版本警告和 `requirements.txt`。
- [ ] **中优先级**：为 Hierholzer、Tarjan SCC、割点/桥和树上差分增加可重复运行的随机对拍脚本。验收：脚本能生成小规模图或树，与枚举算法比较至少 1000 组，并在 README 或审校记录中写明运行方式。证据：本轮仅完成 9 组定向运行用例，见 `ALGORITHM_REVIEW.md`。
- [ ] **低优先级**：评估 P3383 线性筛在 $n=10^8$ 时的峰值内存，并决定是否增加奇数筛/位图版本作为工程拓展。验收：记录实测峰值和题目内存限制；若保留当前 `minPrime` 版本，明确其内存预算。证据：当前 `docs/algorithms/math/prime-sieve.md` 使用每项 4 字节的最小质因子数组。

## 站点内容

- [ ] 重写 `docs/index.md` 的首页引导，使新成员能从首页进入协会介绍、算法教程、题库和贡献指南。验收：四个入口均存在且可访问。
- [ ] 在 `docs/support/index.md` 补充赞助商展示规则和当前赞助信息；没有赞助商时应明确展示联系渠道。验收：页面不再包含无说明的空占位。
- [ ] 在 `docs/about/` 补充社团活动掠影、招新规则和 ACM 校队介绍。验收：导航入口有效，图片来源明确，报名信息标有适用时间。
- [ ] 新增“小白入门”模块，包含环境搭建、基础语法、首批练习和学习顺序。验收：页面加入 `mkdocs.yml`，所有步骤和练习链接可用。
- [ ] 完善 `docs/archive.md`，至少按学期列出已有活动或竞赛成果。验收：可核验条目附有有效来源链接。
- [ ] 在首页或资源页增加常用在线评测系统（Online Judge，OJ）入口。验收：所有外链可以访问且平台名称准确。
- [ ] 新增 ACMM 模板库介绍页面，说明模板范围、使用方式和仓库链接。验收：页面加入 `mkdocs.yml` 且仓库链接可访问。

## 品牌与视觉

- [ ] 完成 `docs/assets/logo.svg` 的 ACMM 四字母设计。验收：在站点页眉的实际显示尺寸下四个字母仍可辨认。
- [ ] 校验 Logo 的对齐、字母间距和透明背景。验收：分别检查浅色、深色背景及窄屏页眉截图。
- [ ] 补齐 favicon 与社交分享预览图。验收：在 `mkdocs.yml` 中配置后构建成功，生成页面正确引用两项资源。

## 构建与部署

- [ ] 审核 `mkdocs.yml` 的主题、插件、扩展和导航配置并删除无效项。验收：`README.md` 记录必要依赖，站点构建无配置错误。
- [ ] 在 `README.md` 增加本地构建与 GitHub Pages 部署说明。验收：全新环境能够按说明完成安装和构建。
- [ ] 在干净环境测试 `mkdocs serve` 与 `python -m mkdocs build`。验收：记录版本、命令和结果，并将仓库自身警告清零。

## 维护规则

1. 开始新版本前先阅读本文件，确认本次计划处理的项目。
2. 两遍自审后，将本次未解决、无法核验或需要后续处理的问题加入“审核发现”，写明优先级和复查证据。
3. 已解决事项不得继续留在 TODO；将解决结果移入 `CHANGELOG.md` 的“未发布”部分。
4. 不把笼统愿望写成任务。每项 TODO 应说明对象、预期结果以及可验证的完成条件。
