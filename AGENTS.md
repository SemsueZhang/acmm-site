# ACMM Site 工作要求

本仓库是算法竞赛协会的教学网站。处理仓库任务时，优先保证内容准确、适合学习、结构一致，并保留用户已有改动。

## 教学文章

1. 撰写或重写算法、技术、课程讲解时，必须使用仓库技能 `.agents/skills/write-academic-tutorial/`，并先完整阅读其 `SKILL.md` 和要求加载的参考笔记。
2. 默认面向正在系统学习算法竞赛的学生，先说明问题与朴素思路，再推导优化；不要直接堆定义和模板。
3. 每个知识点至少包含精确定义、核心推导、正确性或不变量说明、时间与空间复杂度、例题、代码和易错点。
4. 算法正文默认使用 C++17。完整程序必须能够独立编译；变量、下标和区间约定必须与正文一致。
5. 优先选择真实竞赛题作为例题，核对题目链接、数据范围和所需算法。不能只贴题目与代码，必须解释识别线索和建模过程。
6. 明确区分 NOIP/CSP-S 主线和 NOI 级拓展。拓展算法使用独立页面，不得暗示其属于基础考纲要求。
7. 一个页面围绕一个中心知识点。多个算法应拆页，并由索引页组织学习顺序；禁止把整个主题写成缺少层次的算法清单。
8. 只在图能显著帮助理解时使用 Mermaid 或表格。图必须参与解释，不能作为装饰。
9. 可以学习优秀资料的详略节奏、论证方式和选题思路，但不得照抄、近似改写、搬运代码或复刻独特例子结构。

## 资料与准确性

1. 涉及现行竞赛大纲、题目要求、软件版本或其他可能变化的信息时，查阅最新可靠来源。
2. 技术事实优先核对官方文档、竞赛大纲、原题页面或论文。引用链接应靠近所支持的内容。
3. 不把字符串哈希等概率算法描述成绝对正确；不省略影响算法适用性的前提与边界。
4. 不确定的结论应继续核验或明确标注，不得用模糊措辞掩盖推导缺口。

## 修改与验证

1. 修改前检查同目录页面、`mkdocs.yml` 导航、练习清单和现有术语风格。
2. 使用 `apply_patch` 进行手工文件修改，避免覆盖无关用户改动。
3. 完成后检查 Markdown、站内链接和导航，并运行 `python -m mkdocs build`。
4. 对新增的完整 C++ 程序执行 C++17 语法检查和代表性样例；高风险算法应增加边界测试或随机对拍。
5. 明确区分本次新增问题与仓库原有警告，不为消除无关警告而扩大修改范围。

## 完成前自审

1. 任何任务完成后不得立即交付。必须重新阅读用户的全部要求和本文件，将每项要求对应到具体改动或验证证据，检查遗漏、误解和超出范围的修改。
2. 第一遍自审应检查事实、术语、定义、公式、边界、复杂度、代码、示例、链接和构建结果。专业缩写首次出现时给出全称，后续用法保持一致，不使用未定义或含义冲突的缩写。
3. 发现问题后先修正，并重新运行受影响的检查。修正完成后，必须从用户、读者或审稿者视角再做一遍独立复核，不能只检查刚修改的位置。
4. 第二遍复核应再次确认要求覆盖、内容正确性、表达一致性、测试结果和适用范围；未通过时继续修正，不得宣称任务已经完成。
5. 交付前检查 `git diff` 和 `git status`，确认没有遗漏、无关或意外改动，并保留用户已有改动。
6. 最终回复应说明两遍自审和实际验证的结果、自审中修正的问题，以及仍存在的警告或无法核验的事项；无法验证时不得表述为已经通过。

## 版本记录

1. 每次形成可交付版本时必须同步更新仓库根目录的 `CHANGELOG.md`，按“新增、修改、修复、删除”记录本版实际变化；没有内容的类别可以省略，禁止使用“优化若干内容”等无法核验的笼统描述。
2. 尚未发布的变化写入 `CHANGELOG.md` 的“未发布”部分。创建正式版本或合并发布时，将其归入带日期和版本号或提交标识的条目，并重新建立空的“未发布”部分。
3. 仓库根目录的 `TODO.md` 只记录下一版本及后续版本尚未完成的具体事项。每项应包含修改对象、预期结果和可验证的完成条件。
4. 两遍自审中发现但本次未解决、无法核验或需要后续处理的问题，必须在交付前写入 `TODO.md` 的“审核发现”部分，并注明优先级与复查证据。
5. 本次已经解决的审核问题不得继续留在 `TODO.md`；应从 TODO 移除，并在 `CHANGELOG.md` 当前版本中记录解决结果。
6. 提交前同时检查代码或文章、`CHANGELOG.md` 和 `TODO.md`，确保三者描述一致；不补写没有依据的历史变化。

## Git

1. 提交前运行 `git status`、检查差异并只暂存本次任务相关文件。
2. 未经用户要求，不创建提交、不推送、不创建 PR。
3. 用户要求发布时，使用简洁明确的提交信息，推送当前任务分支，并核对 PR 实际包含的提交范围。
