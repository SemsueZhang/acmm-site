# 协作者工作指南

欢迎成为 ACMM 网站的协作者！本指南面向有写作、校对、或维护站点权限的协作者，帮助你快速上手并保持高效的协作流程。

---

## 目标读者
- 拥有仓库写权限或已被邀请为协作者的成员
- 负责内容撰写、目录维护、站点部署或样式/脚本维护的贡献者

## 快速入门（权限与准备）
1. 确认你为仓库协作者或已被授予推送权限；如无权限，请向仓库管理员申请。
2. 克隆主仓库到本地并建立工作分支：

```bash
git clone https://github.com/SemsueZhang/acmm-site.git
cd acmm-site
git checkout -b feat/your-topic
```

3. 建议使用虚拟环境并安装依赖：

```bash
python -m venv .venv
.\.venv\Scripts\Activate.ps1  # Windows
source .venv/bin/activate       # macOS / Linux
pip install -r requirements.txt
```

4. 本地预览：

```bash
mkdocs serve
```

---

## 职责与角色
- **内容作者**：撰写页面、补充算法条目、提供示例题与代码。
- **审阅者 / 编辑**：审阅 PR、检查术语一致性、公式与代码能否正确显示。
- **维护者**：合并 PR、处理 CI、管理部署权限与主题样式。

通常一项改动应至少包含一名作者与一名审阅者。

---

## 分支与提交规范
- 分支命名：`feat/<topic>`、`fix/<issue>`、`chore/<task>`。
- 每个 PR 保持单一关注点（例如：只改一篇文档或只改导航）。
- 提交信息格式建议：

```
type(scope): 简洁描述

更详细的说明（可选）
```

示例： `feat(docs): 新增 KMP 教学页面`。

---

## 文档与内容规范
- 语言：简体中文为主，专业术语可保留英文（首次出现可括注中文）。
- 算法页面标准模板（建议）：
  1. 标题与问题背景
  2. 适用场景
  3. 核心思想与直觉
  4. 分步执行演示（含示例输入）
  5. 伪代码或流程说明
  6. 时间复杂度与空间复杂度（使用 LaTeX 公式）
  7. C++17 参考实现（STL + 中文注释）
  8. 常见易错点与边界情况

- 代码块请使用语言标注（如 `​```cpp`）。
- 图片、CSS、JS 等资源放于 `docs/assets/` 或其他子目录，并在 Markdown 中使用相对路径引用。

---

## Pull Request 流程（审查与合并）
1. 在创建 PR 时，请在描述中说明：改动目的、影响范围、关键实现点（如修改了 `mkdocs.yml`）。
2. 指定至少一名 reviewer（若改动较小可 @ 项目维护者）。
3. 审阅者检查项：
   - 本地 `mkdocs serve` 预览，确认页面显示正常（公式、代码高亮、图片路径）。
   - `mkdocs.yml` 的 `nav` 未产生死链。
   - 文案、术语与风格一致性。
4. PR 通过后，拥有合并权限的维护者可合并；合并时建议使用合并（merge commit）或 squash（视团队约定）。

---

## CI、构建与部署
- 本仓库使用 `mkdocs build` 进行静态构建。
- 请在本地运行 `mkdocs build` 或 `mkdocs serve` 以尽早发现构建错误。
- 若仓库设置了自动部署（`gh-pages` 或 CI），PR 合并后由 CI 执行构建与发布。
- 若你需要更改部署流程（例如自定义 GitHub Actions），请先在 Issue 中讨论并获得维护者批准。

---

## 审阅准则（内容质量检查要点）
- 结构完整：段落清晰、标题层级正确。
- 技术正确：伪代码、复杂度推导、C++ 示例逻辑正确且注释清晰。
- 可读性：公式使用 LaTeX，变量命名一致。
- 可复现：示例与测试用例可在本地运行（当适用）。

---

## 协作沟通建议
- 使用 GitHub Issues 讨论较大改动或方向性问题。
- 小问题或疑问可直接在 PR 评论区沟通。
- 标注（@）相关责任人以便尽快分配 reviewer。

---

## 常见场景快速参考
- **我改了导航但有人说找不到页面**：确认 `mkdocs.yml` 的路径与 `docs/` 中实际文件匹配，注意相对路径与大小写。
- **公式不显示**：确认使用 `$...$` 或 `$$...$$`，并确保 `mkdocs.yml` 已启用 MathJax（项目已配置）。本地 `mkdocs serve` 可用于调试。
- **图片不显示**：确认图片文件在 `docs/assets/`（或相对位置）且 Markdown 使用相对路径引用。

---

## 安全与隐私
- 请勿在仓库中提交密码、API Key、私人凭证或敏感信息。
- 若发现敏感信息被意外提交，请立刻联系维护者并按组织指南执行秘密清理流程（例如使用 `git filter-repo`），不要通过普通 PR 暴露细节。

---

## 维护者联系方式与支持
- 对流程或权限问题，请联系仓库管理员（在 GitHub 上 @SemsueZhang）。
- 需要我（AI 助手）代为格式化或生成模板页面时，可直接在对话中说明主题与内容要求。

---

感谢你的协作与付出！我们希望通过清晰的流程与协作规范，把 ACMM 的知识库打造得对新人友好且便于长期维护。
